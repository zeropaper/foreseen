import Foreseen from '../src';
import freshInstance from './freshInstance';

let instance: Foreseen;

beforeEach(() => {
  console.info('trakak');
  instance = freshInstance();
});

describe('special', () => {
  it('does things', () => {

  });
  it('does things again', () => {

  });
});